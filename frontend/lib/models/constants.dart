class Constants {
  static const String androidLocalHost = '10.0.2.2';
  static const String port = '8080';

  // Change the local host depending on which device/emulator you are using
  static const String apiRootUrl = 'http://$androidLocalHost:$port/api';
}